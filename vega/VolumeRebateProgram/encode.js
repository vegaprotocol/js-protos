/// autogenerated by protoc-plugin-js
const Writer = require('protobuf-codec/encode/writer')
const { uint64, string, int64 } = require('protobuf-codec/encode/types')
const _vega_VolumeRebateBenefitTier = require('./../VolumeRebateBenefitTier/encode.js')

exports.encode = function encode(obj = {}, buf, byteOffset = 0) {
  const writer = new Writer()

  if (obj.version) writer.varint(1, obj.version, uint64)
  if (obj.id) writer.bytes(2, obj.id, string)
  if (obj.benefitTiers?.length)
    obj.benefitTiers.forEach((v) =>
      writer.bytes(3, _vega_VolumeRebateBenefitTier.encode(v))
    )
  if (obj.endOfProgramTimestamp)
    writer.varint(4, obj.endOfProgramTimestamp, int64)
  if (obj.windowLength) writer.varint(5, obj.windowLength, uint64)

  return writer.concat(buf, byteOffset)
}

// export function encodingLength (obj) {
//   throw new Error('Unimplemented')
// }
