/// autogenerated by protoc-plugin-js
const reader = require('protobuf-codec/decode/reader')
const { bytes, uint64 } = require('protobuf-codec/decode/types')

exports.decode = function decode(
  buf,
  byteOffset = 0,
  byteLength = buf.byteLength
) {
  const field$transactions = []
  let field$height = 0n
  for (const [field, { data }] of reader(buf, byteOffset, byteLength)) {
    switch (field) {
      case 1:
        field$transactions.push(bytes(data))
        break

      case 2:
        field$height = uint64(data)
        break
    }
  }
  return { transactions: field$transactions, height: field$height }
}
