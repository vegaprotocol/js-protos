/// autogenerated by protoc-plugin-js
import Writer from 'protobuf-codec/encode/writer'
import { uint64, string } from 'protobuf-codec/encode/types'

export function encode(obj = {}, buf, byteOffset = 0) {
  const writer = new Writer()

  if (obj.epoch) writer.varint(1, obj.epoch, uint64)
  if (obj.assetId) writer.bytes(2, obj.assetId, string)
  if (obj.marketId) writer.bytes(3, obj.marketId, string)
  if (obj.rewardType) writer.bytes(4, obj.rewardType, string)
  if (obj.amount) writer.bytes(5, obj.amount, string)

  return writer.concat(buf, byteOffset)
}

// export function encodingLength (obj) {
//   throw new Error('Unimplemented')
// }
