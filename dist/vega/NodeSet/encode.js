/// autogenerated by protoc-plugin-js
const Writer = require('protobuf-codec/encode/writer')
const { uint32, string } = require('protobuf-codec/encode/types')

exports.encode = function encode(obj = {}, buf, byteOffset = 0) {
  const writer = new Writer()

  if (obj.total) writer.varint(1, obj.total, uint32)
  if (obj.inactive) writer.varint(2, obj.inactive, uint32)
  if (obj.promoted?.length)
    obj.promoted.forEach((v) => writer.bytes(3, v, string))
  if (obj.demoted?.length)
    obj.demoted.forEach((v) => writer.bytes(4, v, string))
  if (obj.maximum) writer.varint(5, obj.maximum, uint32)

  return writer.concat(buf, byteOffset)
}

// export function encodingLength (obj) {
//   throw new Error('Unimplemented')
// }
