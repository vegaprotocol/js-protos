/// autogenerated by protoc-plugin-js
import Writer from 'protobuf-codec/encode/writer'
import { int64, string } from 'protobuf-codec/encode/types'
import * as _vega_commands_v1_OrderSubmission from './../OrderSubmission/encode.js'
import * as _vega_StopOrder_ExpiryStrategy from './../../../StopOrder/ExpiryStrategy.js'

export function encode(obj = {}, buf, byteOffset = 0) {
  const writer = new Writer()

  if (obj.orderSubmission)
    writer.bytes(
      1,
      _vega_commands_v1_OrderSubmission.encode(obj.orderSubmission)
    )
  if (obj.expiresAt) writer.varint(2, obj.expiresAt, int64)
  if (obj.expiryStrategy)
    writer.varint(3, obj.expiryStrategy, _vega_StopOrder_ExpiryStrategy)

  if (obj.trigger?.price ?? obj.price)
    writer.bytes(100, obj.trigger?.price ?? obj.price, string)
  if (obj.trigger?.trailingPercentOffset ?? obj.trailingPercentOffset)
    writer.bytes(
      101,
      obj.trigger?.trailingPercentOffset ?? obj.trailingPercentOffset,
      string
    )

  return writer.concat(buf, byteOffset)
}

// export function encodingLength (obj) {
//   throw new Error('Unimplemented')
// }
