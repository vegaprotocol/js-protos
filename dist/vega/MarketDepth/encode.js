/// autogenerated by protoc-plugin-js
const Writer = require('protobuf-codec/encode/writer')
const { string, uint64 } = require('protobuf-codec/encode/types')
const _vega_PriceLevel = require('./../PriceLevel/encode.js')

exports.encode = function encode(obj = {}, buf, byteOffset = 0) {
  const writer = new Writer()

  if (obj.marketId) writer.bytes(1, obj.marketId, string)
  if (obj.buy?.length)
    obj.buy.forEach((v) => writer.bytes(2, _vega_PriceLevel.encode(v)))
  if (obj.sell?.length)
    obj.sell.forEach((v) => writer.bytes(3, _vega_PriceLevel.encode(v)))
  if (obj.sequenceNumber) writer.varint(4, obj.sequenceNumber, uint64)

  return writer.concat(buf, byteOffset)
}

// export function encodingLength (obj) {
//   throw new Error('Unimplemented')
// }
